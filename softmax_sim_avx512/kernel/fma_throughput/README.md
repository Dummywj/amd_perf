# FMA Throughput

- **语义**：对每个 FP32 元素连续执行 16 次 `value = value * 1.0001 + 0.0003`。
- **输入布局**：`input[0:N]`，一个连续 FP32 向量。
- **资源覆盖**：x86 使用 8 个独立 ZMM 累加器，主要隔离 vector FMA 的发射吞吐；同时包含首尾 load/store。
- **输出**：`output[0:N]`，与输入逐元素对应。

工作负载规模均为 128 的倍数，以保持 x86 独立累加器主体完整。
